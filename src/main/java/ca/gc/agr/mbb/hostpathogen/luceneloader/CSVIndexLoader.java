package ca.gc.agr.mbb.hostpathogen.hostpathogenluceneloader;


public class CSVIndexLoader{
    /*
    public CSVIndex load(final String filename, final String indexKeyField){
	CSVParser parser = null;
	try{
	    parser = fileReader(csvDir, csvFile);
	}catch(java.io.FileNotFoundException e){
	    e.printStackTrace();
	    System.err.println("Unable to find file: " + csvDir + File.separator + csvFile);
	    System.exit(42);
	}catch(java.io.IOException e){
	    e.printStackTrace();
	    System.exit(42);
	}
	CSVIndex index = new CSVIndex();
	index.setIndexKeyField(indexKeyField);

	for (CSVRecord record : parser) {
	    index.add(record);
	}
	return index;
    }
    */
}
